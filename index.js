import React from "react";
import ReactDOM from "react-dom/client";
import GameMockup from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <GameMockup />
  </React.StrictMode>
);
